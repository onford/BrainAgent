# 当前实施范围与进度

2026-09-12：按用户最新指令，以共享预处理方法、真实来源、固定 EEGNet 三种子主指标与 CSP-LDA 对照推进剩余修改。

已撤回 I30（接近前沿性能）、I34/I35/I53–I56（逐被试拟合、个体预处理及其在线/记忆扩展）。不将它们计入未完成目标，不恢复旧探索。被试标识、分组交叉验证及按被试汇总评价保留，用于避免泄漏和正确统计；记录内必要的 ICA/ASR 等算法拟合不变成按人选择方法。

原路线图是历史审计，不再直接作为当前目标。当前逐项状态及依赖见 [实施台账](sources/remaining-modifications-20260912/status.json)。A 的原验收只验证原版本；代码修改后必须重新验证，不能引用旧运行来证明当前代码。

逐被试策略、拟合执行、候选编辑、变换导出及界面入口已移除。当前评分直接使用共享流程产生的物理 V 数组，旧协议产物保留只读。后端全量回归 1109 项、前端 153 项、Sites 转发 5 项均通过，前端构建通过。

检索增加持久化动作预留、完成结果恢复、去重重用和绝对截止时间；不确定请求保留未知结果。模型调用账本记录重试次数、返回 token 用量及耗时，未知金额不填零。原生调用接入进程树取消/超时回收，保留执行日志和状态。

经典流程接口增加 PREP 和 Automagic 完整 Project/Block 原生链，当前清单为 53 单元、90 操作、309 profiles。PREP 与 Automagic 固定随机策略在 S001R04 的图/Codec 输出均与独立原生输出逐值相同（64 × 20000，最大差 0 V）。Automagic 的原始时钟随机策略单列；固定策略和 REF 投影显式记为工程派生，保留原生 65 通道、自动坏道插值、QC 与每阶段数组。HAPPE 缺少 Optimization Toolbox；RELAX 尚待原生配置/依赖验收。

源码统一 LF，历史证据文件按字节保留；全部 53 单元的源码哈希已与 Git 暂存内容核对。1109 项全量回归之后的原生链修订另通过 101 项专项回归。当前原生对比引擎：`93dff8d40477e99d670facdf352dd8af85bd02a5dd700b9948459ef1c84fe82c`。

全量 EEG、独立数据、强神经保护、其他剩余修改及公开服务更新尚未完成；原数值 profile 验证随引擎变更已失效，须重新发布当前版本证据。逐项状态见实施台账。
