# 当前实施范围与进度

执行顺序已按用户要求调整为[核心交付计划](core-delivery-plan.md)：34 个核心条目优先，15 个优化与扩展后置，Sites 1 项暂缓；原验收状态不因排序而改变。
> 2026-09-13 范围更新：保留首次推荐，移除后续模型自主调整。当前规则见[首次推荐与固定执行](fixed-initial-recommendation.md)。本文中的历史自主调整验收不再作为当前目标。

最新能力、实际服务及尚未验收范围见[现行入口](current-capabilities.md)，逐项状态见[实施台账](sources/remaining-modifications-20260912/status.json)。以下按发生顺序保留历次记录；后续记录取代此前的“当前”状态，不重算历史测量。

2026-09-12：按用户最新指令，以共享预处理方法、真实来源、固定 EEGNet 三种子主指标与 CSP-LDA 对照推进剩余修改。

已撤回 I30（接近前沿性能）、I34/I35/I53–I56（逐被试拟合、个体预处理及其在线/记忆扩展）。不将它们计入未完成目标，不恢复旧探索。被试标识、分组交叉验证及按被试汇总评价保留，用于避免泄漏和正确统计；记录内必要的 ICA/ASR 等算法拟合不变成按人选择方法。

原路线图是历史审计，不再直接作为当前目标。当前逐项状态及依赖见 [实施台账](sources/remaining-modifications-20260912/status.json)。A 的原验收只验证原版本；代码修改后必须重新验证，不能引用旧运行来证明当前代码。

逐被试策略、拟合执行、候选编辑、变换导出及界面入口已移除。当前评分直接使用共享流程产生的物理 V 数组，旧协议产物保留只读。后端全量回归 1109 项、前端 153 项、Sites 转发 5 项均通过，前端构建通过。

检索增加持久化动作预留、完成结果恢复、去重重用和绝对截止时间；不确定请求保留未知结果。模型调用账本记录重试次数、返回 token 用量及耗时，未知金额不填零。原生调用接入进程树取消/超时回收，保留执行日志和状态。

经典流程接口增加 PREP 和 Automagic 完整 Project/Block 原生链，当前清单为 53 单元、90 操作、309 profiles。PREP 与 Automagic 固定随机策略在 S001R04 的图/Codec 输出均与独立原生输出逐值相同（64 × 20000，最大差 0 V）。Automagic 的原始时钟随机策略单列；固定策略和 REF 投影显式记为工程派生，保留原生 65 通道、自动坏道插值、QC 与每阶段数组。HAPPE 缺少 Optimization Toolbox；RELAX 尚待原生配置/依赖验收。

源码统一 LF，历史证据文件按字节保留；全部 53 单元的源码哈希已与 Git 暂存内容核对。1109 项全量回归之后的原生链修订另通过 101 项专项回归。当前原生对比引擎：`93dff8d40477e99d670facdf352dd8af85bd02a5dd700b9948459ef1c84fe82c`。

全量 EEG、独立数据、强神经保护、其他剩余修改及公开服务更新尚未完成；原数值 profile 验证随引擎变更已失效，须重新发布当前版本证据。逐项状态见实施台账。

后续修订：补读结果复用与逐尝试模型预算已接入正式工作流、方法入库、关联搜索及指标解读。默认上限为 128 次传输尝试、累计 1600 万输入字节、单次 75 万输入字节、单次 32768 输出 token、累计预留 4194304 输出 token、绝对 6 小时。失败与不确定请求保留预留；这些是运行限制，不是金额估计。实际 token 按服务返回记录，未知费用不填零。

首批提交 `7f4a490` 的 fresh 真实验收在接入核对失败：模型把来源的入选被试范围误当成排除名单。没有进行数据排除或数值搜索。现已加入来源对象性质与原文短句校验，并补齐预算文件展示；修正后 118 项回归、前端 153 项测试与构建通过，真实复验待完成。两次 Git 推送均因网络故障失败，本地提交保留。

第三批修订补充检索停止审计、来源访问等级与报告链接，修复 GitHub 限流判定和过早重试（102 项回归通过）。第二次 fresh 验收发现排除动词与英文名单解析不足，已修复；次要来源主张无法确认时保留原提案并清空有效排除编号，结构/标签矛盾仍阻塞。相关 36 项回归通过。两次失败验收均保留，未作为闭环成功证据。当前提交另有 `f4528a1`，第三次推送仍因连接重置失败。

第四批修订（146 项专项回归通过）：结论资格作为确定性产物进入搜索报告、工作流报告、叙述上下文与交付校验，绑定选中 assessment 和协议哈希。完整开发评分不提升为神经保留、独立泛化或预处理因果收益；历史指标保留原协议标签。交付只核验并携带选中回执，因此开发差值在同时核验两个回执的搜索报告中展示。再次清理神经先验和报告模板中遗漏的被试拟合权限与标题。

`e8d11f4` 的 fresh 验收 `36b3563553c448129c7aa6f66da3e019` 在三方数据核对达到模型输出上限，未进入数值评价：完成 token 32778，其中 reasoning token 30464。现压缩重复比较 Schema、移除该决策上下文的重复原始工具响应（来源正文、全部字段与原始存档保留）；截断回复不解析、不接受，最多允许一次显式精简修订，继续计入原持久化预算。该修订的真实闭环仍待验收。跨格式版本恢复被正式接口拒绝，已有检查点未被改写。

第五批规则修订：普通配方与直接 v2 来源图共用执行规则审计，按真实信号祖先和模型/诊断依赖判断次序及前置条件。跨实现匹配需要显式版本声明；相关但未绑定的实现记录覆盖缺口，不把现有 ASR 实现限制外推到其他变体。每条规则保留修订号、撤回理由和替代关系，编译产物保存规则及配方哈希、条件结果和相反软规则的冲突记录。相关 67 项测试分批通过；全部研究知识尚未转换为可执行谓词，I16–I18 仍为部分完成。

RELAX 原生进展：新增隔离下载的 Picard 2.0、FieldTrip-lite 20250523 和 MWF 源码快照。核对上游后在单次运行副本修正 EEGLAB 2024.0 的 `eegrej` 标量循环上界，原依赖不变。显式采用 75 Hz 低通（160 Hz 数据的工程派生配置）与 60 Hz 工频，两次原生计算得到相同的有限 `62 × 18240` 输出，最大差 0 V；原输入为 `64 × 20000`。完整驱动在第 4 次运行成功，前 3 次失败/后处理错误均保留。删除时间段、通道与新增边界事件的桥接尚待实现，不能以该结果宣称已进入统一评价。

第六批：完整 RELAX 已接入统一图与 Codec，当前为 54 单元、91 操作、310 profiles。显式工程派生配置在 S001R04 与独立原生输出逐值一致（62 × 18240，最大差 0 V）；15 个目标事件保留 13 个，均已验证到原始样本位置的映射，删除通道/区间和边界事件保留。此证据只覆盖一个记录与配置，不作为完整固定面板效用或神经保护证据。当前 RELAX 验收引擎为 `fb841140626a80355cd39ed6ea47eb7028949b729e679a518ca33bf55a5e814d`，见 [验证摘要](sources/remaining-modifications-20260912/native-relax-graph-2-verification.json)。

`e7590f4` 的 fresh 运行完成真实调研、接入和基础候选评价（4 人、12 记录、180 Trial；EEGNet 三种子开发 BA 0.498435441370224），随后累计模型输入预算耗尽。没有实质文献候选进入评价，因此验收失败。重复算子合同现以精确引用无损合并；仅导航决策使用明确标记的短预览，方法提取与核验仍读取来源正文。预算耗尽直接停止并保留已完成测量，报告可在不生成模型叙述时交付确定性测量和结论限制。专项初测 92 过、1 败；修复预算被误重试后搜索控制器 33 项通过，全量回归待完成。

版本同步更新：此前网络失败已经恢复，`7f4a490` 至 `0f55219` 五批提交均已推送到 `origin/dev-xx`；第六批尚在验证。上文各次失败推送和失败验收均为历史记录。

第六批全量回归已完成：1159 项通过、109 条警告，754.96 秒。首次收集因旧测试引用已删除的规则函数而失败，测试迁移到统一规则审计后重跑全量通过；失败日志保留。54 个单元源码哈希全部与暂存字节一致。

第七批：共享流程目标贯穿检索、筛选、方法提取和修复；依赖逐被试归一化/拟合的来源保留超出目标的原因，不以删除必需阶段来伪造可执行方法。参数核验新增英文阶数、千位数字与明确事件相对起点的可审计解析，独立语义 unknown/unsupported 不被词法匹配覆盖。文献证据索引与数据事实、论文参考编号明确分开，补读和恢复后同步索引。58 项专项回归及5项补读回归通过；RELAX 在 `718458cfc37b338219798f0e1394385cbf54549f2dfa2116c319505661bf5170` 引擎下再次逐值一致。见 [来源数字审计](sources/remaining-modifications-20260912/source-numeric-audit.json) 和 [新原生复验](sources/remaining-modifications-20260912/native-relax-graph-3-verification.json)。

第八批：供应商冷却持久化并跨工作流共享；搜索快照按用户/凭据/参数隔离，最多128项/64MiB、默认最多600秒，实际期限服从服务的更短有效期和缓存禁令。相同查询并发合并，成功缓存有获取时间、年龄和原结果哈希；失败与缓存命中分别记录真实传输尝试数，未知网络结果不伪装成未发送。85项集成/工作流回归通过，之后74项相关回归与17项状态测试通过（批次重叠，不累加为独立覆盖）。两次真实供应商实测中，Crossref/Europe PMC/OpenAlex复用已确认，GitHub/arXiv访问成功，限流冷却生效；Unpaywall缺联系邮箱配置。来源正文达到80000字符截断，仍按部分访问记录。见 [真实配置实测](sources/remaining-modifications-20260912/provider-live-2-summary.json)。缓存有效期按 [RFC 9111 的响应有效期与年龄规则](https://www.rfc-editor.org/rfc/rfc9111.html#section-4.2)作保守限制。

第九批：末端纯分段方法保留原始源窗口，同时从同一份已处理连续信号建立明确标记的工程评分分支。仅允许补入因记录边界缺少源上下文的事件；有源拒绝标记或其他筛除原因时阻塞。分段后基线与拟合流程仍完整执行再裁剪。评分窗源节点在编译与配方哈希中保留，质量和重建器按实际评分依赖链定位分段边界，全部分支日志继续核验。106 项专项测试通过；合成 BIDS 集成保留全部 78 个冻结 Trial，原始各 25 个宽窗 Epoch 分别保存，三轴均可评价。该测试只验证执行合同，不是科学效益或正式文献闭环证据。最新正式运行仍使用 81ac1a1，宽窗文献候选的不可比记录保留；全量回归和新版本真实验收待完成。GitHub 连通故障使 0f55219 之后的远端状态尚未确认。

第十批：状态化 train/calibration 分区拟合使用独立执行器，先裁合法区间，再重放信号、模型、诊断和参数端口的完整依赖。源全记录状态不复用；不同显式区间和已有人工决定继续阻塞。非零起点测试中，改变区间外信号会改变全记录模型，但分区目标模型逐位不变；原有 record-local 处理分支行为保留。重放上下文、模型、决定和执行日志均落盘验签，资源预留加入重放工作量。34项专项回归及8项最终分区/资源回归通过（覆盖重叠）。最新引擎的全量回归和原生配置复验待完成。

第6次 fresh 验收在81ac1a1完成六阶段交付：6个控制/派生候选完成评价，实质文献候选因固定Trial缺失不可比，文献闭环验收仍失败。预算耗尽后保留已完成的评分与确定性报告；原始数据和运行代码哈希不变。见 [原始验收结果](sources/remaining-modifications-20260912/live-integrated-6-result.json)。第7次 fresh 验收运行在7401c8f固定快照；不改写前次结果，也不把开发集最高分提升为泛化或神经保护结论。

7401c8f 全量回归结果为1184通过、1失败、109警告（802.12秒）。失败测试误把0.2秒内最多3个并发请求断言为必须启动3个；实际启动1个后正确超时。测试现检查并发上限、已开始请求的取消、全部未读来源及停止原因，不再依赖磁盘/CPU速度；相关12项回归通过。生产预算和取消逻辑未因此放宽，最新版本全量复验待完成。

第十一批：按当前42来源、65规则、24算子类和276关系生成冻结知识覆盖与议题队列，保留121个关系缺口及所有规则/参数的条件与反证检查。有限执行谓词、条件建议与仅目录收录分开记录；绑定不提升为整条结论已执行或已验证。产物哈希进入搜索协议，篡改会在候选执行前阻塞。神经能力清单从旧线性操作表改为完整91操作/310 profiles，并保留真实依赖可用性边界。56项回归及后续19项测试通过（重叠）。见 [目录覆盖审计](sources/remaining-modifications-20260912/knowledge-coverage-audit.json)。该批没有声称新增证据已填满研究缺口。

RELAX在分区引擎1d7369f5833045488d7d404615829f858750b402df6d0043268941d56846c2ba上再次与原生逐值一致，62×18240、最大差0 V、15目标事件保留13，原数据和依赖不变。见 [原生复验4](sources/remaining-modifications-20260912/native-relax-graph-4-verification.json)。仍只覆盖一个明确配置，不作为完整固定面板或科学收益验收。

2026-09-13：bacbb66全量回归1190项通过、109警告（799.38秒）。该证据覆盖分区重放和评分窗口，不涵盖随后新增知识覆盖与规则例外。

第十二批：规则作用域获得任务、数据集、物理电压及共享配方事实。经验性软规则例外必须有机器条件与证据；未知条件不跳过规则，硬条件不能被经验例外禁用。相反次序及无歧义requires/incompatible关系形成带实际节点和硬约束优先结果的冲突集；多实例依赖不被误解为必须依赖全部节点。明确null参数与缺失/运行时占位符区分，固定绑定参数也可作为条件。71项规则/控制器回归通过，另31项含图数值集成测试通过（重叠）；复杂参数冲突与知识失效联动仍在后续范围。

第7次fresh流程发现PDF连字ﬁfth-order无法通过阶数核验。修订只展开拉丁连字，保留原文与坐标，不转换数学上标或补造单位；16项语义/恢复测试通过。[只读词法重放](sources/remaining-modifications-20260912/pdf-ligature-audit.json)证明数字检查的改动，历史unsupported状态保持，不能据此提升旧流程为验收成功。

第十三批：冲突集补充对同一实际变量的互斥参数要求，包括数值上下界、开闭边界、等值、排除值及有限选项。只在能够证明无共同取值时记录参数冲突，不把未知类型或不同节点的要求混合。31项规则/配方测试通过。GitHub连接已恢复，远端dev-xx核对到30b9f1c33cc830092eb0667060c185af18ac8c0f；此前0f55219之后的积压提交已同步。新提交仍按版本管理约定继续推送。

第十四批：知识目录新增用户隔离的不可变修订、并发比较更新、来源/规则停用与恢复、正文勘误的执行绑定复核，以及内置目录升级的显式合并。失效依赖影响新搜索和普通预处理的计划/提交/重试/发布，独立实现与数学硬约束保留；冻结搜索及子进程使用原修订。历史影响报告只读列出需重评范围，无法验签的历史另行标记。真实文献状态没有被合成测试改动。见 [修订接口与边界](knowledge-revisions.md)。最后一批111项测试通过、2条弃用警告（176.52秒）；此前52项规则/神经建议/图执行测试通过（覆盖重叠）。一项中途源码变化触发的完整性阻塞已记录，并在停止源码编辑后的最终批次通过，未放松保护。

远端已同步到6849f96（第十三批）。第8次fresh流程在30b9f1c固定快照上完成了MOABB事件/窗口来源候选评价；该候选不含实质信号处理，不能据此声明文献闭环通过。第7次流程继续保留其来源核验失败，不改写历史结果。接下来处理物理输出、共同视图和移除信号对照。

第十五批：原生配对比较改为核验逐记录物理测量合同，不再依赖少数算子名称；模型拟合旁支不改变最终频带历史。新增相同网格的共同平均参考/分析滤波视图及有符号差值，完整数组验签保存，质量页面提供同尺度波形和下载。对改变时间网格或缺少内部映射的原生流程保留不可比原因；差值不被解释为纯伪迹或神经真值。见 [对照合同及限制](physical-signal-comparison.md)。最后综合批次82项通过，1项新测试夹具漏填必需证据字段；补齐夹具后4项专项通过（覆盖重叠），155项前端测试及构建通过。真实12记录/180试次只读复核的差值最大算术误差0 V，历史产物和源码不变。

第7次fresh六阶段交付结束，8个候选完成评价，但没有实质文献候选，mechanical_gate与acceptance均为false，原数据和运行代码不变，见 [原始验收结果](sources/remaining-modifications-20260912/live-integrated-7-result.json)。第8次尚在运行，现有门槛已正确拒绝把仅有事件/分段的来源候选计为实质文献处理；此前进展表述已更正。知识修订提交4a6afb4首次推送网络失败，随后重试成功同步到远端。

第十六批：指标解读显式区分测量描述、限制与待验证假设；假设必须有前提、竞争解释和预测，所声明的依据与引用接受确定性可用性校验。PSD事实从实际频率轴和保存曲线计算，总方差不授权频谱归因，数值秩不证明严格零特征值。PSD曲线与频带标量的不同归约方式写入上下文，新运行解读同时检查冻结来源停用状态。80项最终相关测试通过，前端156项测试与构建通过。三项真实回复的关键数字与缺测原因已复核，但同模型复核不作为独立专家验证，详见 [解释证据边界](metric-claim-evidence.md)。

d7c734f固定版本全量回归1222通过、1失败、109警告（834.35秒）。唯一失败是测试子进程按GBK读取UTF-8计划，尚未开始进程守护；修正测试显式编码后，包括父进程退出与无孤儿进程在内的最终80项批次通过，生产守护未放宽。第十五批已推送d7c734f，最新解释修改尚在提交。

第8次fresh已完成六阶段交付，因模型预算耗尽停止搜索，选中MOABB事件/窗口来源候选；substantive_literature_evaluated_ids为空，mechanical_gate和acceptance均为false。原数据及运行代码不变，见 [第8次原始结果](sources/remaining-modifications-20260912/live-integrated-8-result.json)。该结果继续保留为文献执行闭环未通过，后续工作不能通过改写验收门槛解除它。

第十七批：诊断注册冻结输入域、标签权限、数值合同、预算和产物，新增实际保存PSD的频谱分布内核。执行前登记竞争假设和三个结果分支；下一次模型决定必须遵循实际分支或显式修订。新质量回执增加缺测索引，频谱不再为一条汇总曲线加载全部波形明细。最后83项后端专项、157项前端测试及构建通过。真实复核初次超出输入预算，修复后沿用原模型预算与截止时间通过；仅验证诊断和下一动作提案，不表示后续预处理已执行，详见 [诊断合同](diagnostic-registry.md)。

626d825固定快照全量后端1227项通过、109警告（829.53秒），覆盖第十六批及此前修改。第十六批两次推送因GitHub网络连接失败，远端仍须重试确认；不把本地提交视为已同步。

第十八批：复查发现评价指南仍有允许目标被试EA的旧段落，重建回执仍用before_parent_EA范围名；均已替换为共享物理流程。当前报告不再展示适配前表示说明，历史v1保持明确的历史解释。补标两份旧神经先验材料，并从I20待办移除个体峰频拟合目标。95项重建/报告/效用回归通过（26.70秒）。第十六、十七批已成功一起推送，远端确认为c44bbdc。

第十九批：按I20的验证依赖先补I25。新增固定球形头模型的已知源、电极电压及全部哈希，保持真实EEG cleanproxy原合同独立。官方A01T.mat与本地42806453字节文件逐值一致后，固定无任务EOG窗口生成记录波形模板，未使用MI类别或个体拟合。最终84组结果覆盖12种污染/强度条件、5个解析对照和2个注册滤波配置；全部产物验签、原数据不变，117项回归通过。此为有限数值验证，更多原生方法、头模型、伪迹模板和真实神经保护仍待完成，见 [验证合同](known-source-validation.md)。首次实测后的审计脚本路径错误在独立后处理修复，未重跑或改写已完成测量；最终固定版本另有完整验证3。

第二十批：共同物理数组的时序/形态诊断进入注册表，明确未对齐误差、增益、相关峰偏移与能量时间重心；周期歧义、搜索边界和缺测不提供虚假的点估计。完整记录/试次/通道分母与单位、样本数验签，页面分开显示变化和不可辨识。最终95项后端专项、158项前端测试及构建通过。12记录/180试次的真实只读复核读取62.2MB，4.172秒完成，历史文件和源码不变；只支持共同视图的描述性变化，见 [时间测量合同](temporal-preservation.md)。I20的周期/非周期及侧化部分继续实施。

651c01c 固定快照全量后端 1263 项通过、109 警告（821.01 秒）。第二十批两次推送分别连接失败/重置，尚未确认同步。

第二十一批：新增固定连续窗口及受预算约束的周期/非周期频谱诊断，显式记录频带、重采样支持、负差分、完整分母与局部覆盖。73 项后端相关测试及额外 1 项质量绑定集成检查通过（覆盖重叠）；159 项前端测试与构建通过。12 条真实记录只读复核中，原始阶段可测，8–30 Hz 处理阶段因频带不足不可用；两次诊断各读取 17.6 MB，历史和原始数据不变。首次驱动的精简面板 Schema 错误保留，第二次读取验签后的完整面板通过。见 [频谱合同](spectral-components.md)。继续侧化/基线不确定性与 I22。

第二十二批：固定 C3−C4 μ/β 配对 ERDS 诊断接入，保留真实提示前基线、逐试次功率/差值和完整记录/参与者分母；留一均值范围明确是敏感性，不是置信区间。74 项后端专项通过，前端 160 项通过，模板索引类型修正后 6 项组件复测及构建通过。4 人/12 记录/180 试次只读复核完成，原文件和代码不变；β 留一参与者均值范围跨零，不声称稳定侧化。见 [配对差值合同](sensor-lateralization.md)。第二十一批推送仍遇连接重置；继续 I22。

第二十三批：v2 每步保存不可变检查点和工程观测；声明的条件失败先保存证据再暂停。新增验签复核、API/agent 的局部步骤替换与共享新计划，保留原停止条件和所有原始记录，重新检查当前适用性，从原数据完整重执行；无中间拟合状态移植。88 项后端专项、161 项前端与构建通过。12 条真实记录的工程裁剪练习全部触发暂停，替代图全部完成且与原数据同窗裁剪最大差 0 V，旧状态/文件/原数据不变。此为恢复机制验收，不表示裁剪具备科学收益，详见 [复核与替代合同](step-review-and-shadow.md)。第二十二批组件复测数校正为日志中的 6 项。

第二十四批：本地结构化元数据、缺项与模板坐标声明进入观察和报告；全部 1526 组 EDF/WFDB 独立比较得到 1260 一致、266 注释数量不同。3052 个源文件均符合官方发布清单，保留差异而不自动排除。29 项相关测试通过、2 警告、55.69 秒；真实检查 132.828 秒，原数据与冻结代码不变。I40 的继承、适用关系和非结构化元数据仍待实现，见 [核查合同](local-metadata-checks.md)。

ad18d2b 固定快照全量后端 1306 项通过、109 警告、835.95 秒。第二十三批推送因连接失败尚未确认同步；继续 I41 官方 BIDS 校验。

第二十五批：固定官方 BIDS Validator 3.0.1、Deno 2.9.6、BIDS 1.11.1 / Schema 1.2.7 在新输入注册前离线检查全部文件和 TSV 行。全量 EEGMMIDB 的 1526 记录、11013 文件通过检查，39569 源事件完整保留、4918 训练试次不变，0 错误、70743 推荐元数据缺项警告逐组保留理由，403.406 秒，原数据和冻结代码不变。未知警告待复核，错误或不完整结果阻止注册；长表第 1003 行错误已由真实校验器检出。29 项最终专项通过；额外产物/进程/排除证据批次 27 项通过（覆盖重叠），见 [官方校验合同](official-bids-validation.md)。第二十四批推送仍因 GitHub 连接失败，后续重试确认。

第二十六批：记录级文献提案补完整 ID 字面核对，未定位通道/试次和重复编号保留为未确认；18 项专项通过，相关产物/进程组合 27 项通过（重叠）。第7次真实调研的全部两条提案只读复核，引用均可在保存正文定位；56 条记录仅匹配文献标记，另一条未确认，自动排除为 0，历史不变。I42 的独立语义对应与事前科学质量规则仍未完成，见 [对象证据边界](exclusion-object-evidence.md)。第25批全量官方校验本身 22.172 秒、进程树峰值 RSS 416624640 字节，转换/检查全链总计 403.406 秒。

第二十七批：artifact-codec-2 为底层容器增加封闭字段/版本、能力 API 共享 Schema、完整对象/稀疏/信号/事件轴校验。首轮误拒 MNE 命名整数导致 21 失败，修正数值序列化后 41 通过，最终扩展批次 77 通过、2 警告、65.64 秒。13 个真实历史描述只读解码/独立重编码完整指纹一致，12 条信号与原独立数组最大差 0 V，原文件和冻结代码不变，10.296 秒。I44 的诊断科学语义仍未全部统一，见 [合同与限制](codec-contracts.md)。第26批推送遇连接重置，尚未确认远端同步。

第二十八批：真实 MATLAB/RELAX 滤波后取消、超时、执行器强制退出均验证本次进程句柄退出。新增独占 worker 锁下的不可变中断回执、原生最后状态保留及中断产物可见性。实际同一配方的两记录任务崩溃重启后 attempt 为1/2，已完成记录和旧原生回执不变，171.172秒；最终44项专项通过、2警告、44.80秒。监测文件共享冲突及驱动误用列表顺序的失败均保留，未修改算法以通过检查。见 [生命周期合同](native-lifecycle-and-recovery.md)。

2bb79cd 固定快照全量后端 1335 项通过、109 警告、872.51 秒。安装 inspection/dev 依赖后补齐本机缺少的 aiohttp/attrs 等声明依赖，pip check 无依赖冲突，数值计算版本未升级。第27批推送出现连接中断且远端核对连接失败，尚不能声称已同步。

第二十九批：工作流/搜索跨后端持锁覆盖实际执行和终态发布，持久停止请求与恢复代次保留预算，线程取消排空后才释放锁。54 项最终后端相关测试、2 警告、141.61 秒；前端 162 项及构建通过。独立 Windows 多进程 7.75 秒实测验证互斥、远程取消与写入排空，冻结源码不变；不代表模型科学链验收。见 [跨后端生命周期](multi-backend-lifecycle.md)。继续 I32 全面板单候选资源成本测量。

399e152 固定快照全量后端 1351 项通过、109 警告、910.30 秒。第28批推送仍因 GitHub 连接失败，未确认远端同步。

第29批补充：历史搜索协议在控制入口写锁前被拒绝，取消/重试/启动/自动恢复均不能改写旧记录；只读访问保留，10 项专项通过（8.85 秒）。第29批及之前积压提交已推送并核对远端 `2ff9b59`。I32 的 109 人/327 记录基础候选正在固定副本上实测，未减少五折、三个种子或训练配置；完整结果仍待结束。

第29批前端收尾：v2 历史报告继续可读，但仅当前 v3 显示运行控制，164 项前端测试和构建通过。`6ce5e60` 已推送并核对远端。I32 全量成本的原始测量备注记录共享主机上同期目录检索和专项回归活动，不将墙钟值声称为空闲主机基准。

第30批测量工具：Windows Job 计量保留退出子进程 CPU/I/O，完整候选驱动拒绝输入输出重叠和不足范围；正式验收区分全集参与者与面板并核对文件增删、全部应用与驱动。4 项专项通过。全集成本运行已完成 15 次 EEGNet 训练，但质量与重建尚未结束；原 7200 秒预算保持，不能提前宣称完整成本或 agent 验收。见 [测量范围](full-dataset-cost.md)。远端已核对到 1ba1d5f。

第30批补充：新评价为效用、质量和重建保存独立资源观测，异常与正常返回分开，当前进程 CPU 不冒充子进程总 CPU，缺测 I/O 等待保持 null；观察文件接受整体清单验签。54 项相关测试通过（26.10 秒，覆盖重叠），当前全集运行仍使用原冻结代码。87f0aab 已推送并核对远端，固定快照全量后端回归进行中。

第30批预算衔接：新工作流可显式冻结模型请求的绝对总时限，默认六小时及请求/输入/输出限额不变；计时包括数值等待，重试不续期。15 项模型预算、范围与执行锁专项通过（2.21 秒）。a5af55d 第一次推送失败、第二次成功，远端核对随后记录；全集成本仍未结束。

87f0aab 全量后端回归 1367 通过、2 失败、109 警告（870.79 秒）。失败显示历史工作流 retry/run 在拒绝执行前创建 workflow.lock，违反严格只读；修正为写锁前检查并保留锁内复核，17 项相关专项通过、2 警告（10.56 秒），原测试保持。完整目标文献输入的只读序列化预检为 327961 字节，另 Schema/指令 16320 字节，低于现有单次 750000 字节上限；没有把历史来源复用为 fresh 验收。

I32 基础候选在原 7200 秒预算内结束，墙钟 6995.450 秒，15 次 EEGNet 共 438 epochs，全部 4918 试次和 327 记录完成质量检查。重建 109 个分配案例中 92 可评价、17 脉冲案例不适用（部分冻结试次的共同频带注入量不足），三轴状态 partial 不改写。数据与冻结代码不变；共享图成本仍在测量。见 [成本及科学覆盖边界](full-dataset-cost.md)。另补齐工作流关联旧搜索的写入前保护，27 项相关测试通过、2 警告（194.92 秒），旧 v1/v2/缺失版本均不能借工作流恢复重新执行。

I32 图组件测量结束：全部 327 记录/109 人，执行 2482.143 秒、核对 138.727 秒，与基础配方最终物理数组最大差 0 V，输入和冻结源码不变。输出约 28.217 GiB，Job CPU 1499.609 秒，采样 RSS 最大 783736832 字节。含 cProfile 与共享主机活动，不能把与基础四进程执行的时间差全部归因于保存。最新 ad56f28 完整后端回归 1377 通过、109 警告（855.40 秒）。I27 仍未开始第9次 fresh 全集流程，实质文献闭环尚未验收。

第31批：重建v5采用固定四分之一秒、单位RMS的脉冲块，最后按整个源记录缩放；保留原分配、种子、共同空间、阈值和分母。新版完整重建专项109人/109案例/1640探针试次全部可评估，109次干净及109次污染回放、111.408秒，数据/历史基线/冻结代码不变。203项最终专项通过，另2项审计测试验证失败候选保留证据且不隐藏缺失或篡改。原型102可评估/7例全零短块失败和旧基础三轴partial均保留。训练模板的旧适配说明已清除。见 [探针复核](sources/remaining-modifications-20260912/probe-v5-review.json)。

I27 第9次fresh全集流程（560f5ec，80c33ed4a18148428944b2536a42c839）已真实通过API新建，但第一次研究规划请求超过原750000字节上限，在数值搜索前失败。数据和冻结代码不变，验收仍false。定位到1526条WFDB比较重复观测字段后，改为9组精确共享内容并保留所有记录和文件哈希，完整请求封装从1125408降至370089字节，1260一致/266不一致完全还原。21项相关测试通过、2警告、2.56秒；预算上限不变。第10次新流程待创建，见 [上下文预检](sources/remaining-modifications-20260912/wfdb-context-preflight.json)。

第10次fresh流程在2906731冻结副本上已通过真实模型请求，工作流e1adc0309f114a3eb6406c8c7abd5094，已完成数据调研、正在数据接入，数值搜索与实质文献执行尚未完成。保持第9次的预算和数值配置，未续写其失败回执。

I43工具准备：新增只读检查点审计，独立复算物理数组/事件关联、冻结内部划分、训练范围内归一化和逐试次推理。历史完整基线的109人/327记录/4918试次/15模型通过，最大概率差约1.11e-16，708个读取文件不变，27.757秒；原三轴partial保持。另2项篡改、字节数及越界测试通过（1.12秒）。新赢家的ZIP、导出数组与真实训练示例仍待正式结果，见 [检查点核验](checkpoint-audit.md)。

I27第10次已完成调研和接入，进入109人/327记录/4918试次的数值搜索，当前执行基础运动频带＋平均参考候选。实质文献候选的数值执行、后续决定和最终交付尚未完成，冻结代码2906731及预算保持。

I43交付工具补齐：历史第8次包的157个ZIP成员、4人/12记录/180试次、物理数组与事件映射逐项核对，独立目录中的训练示例实际运行通过，9.110秒，31个读取文件不变。错配模板与越界ZIP两个反向控制均被拒绝，原始ZIP不变。该历史运行的科学验收仍为false，正式赢家仍待第10次结束。见 [交付核验说明](checkpoint-audit.md)。检查点提交8c6a38b的前两次推送连接超时，尚待重试核对。

I46构建保护：新工作流绑定源码、登记依赖版本与Python平台，旧构建/缺少绑定的运行在恢复、重试和直接阶段执行前保持只读；同一服务启动后文件或环境改变也拒绝新执行。28项后端通过、2警告、238.86秒，166项前端及构建通过。首轮在数值测试未结束时被中止，日志保留；第二轮线程栈显示数值等待和ZIP写入，最终全部通过，未放宽断言。API与Worker启动脚本改用同一干净提交副本，实际服务更新和浏览器核对仍待进行。公开8001只读核查工作流、搜索、会话及数值任务均为空；8000探测超时，保留其进程。Git远端已核对f6f649f。第10次全量基线已结束预处理，正在评价。

I46实服务更新：公开8001的API与Worker现使用8d99111固定副本，health、build-info及能力目录均200，目录包含54单元/91操作/310配置。旧进程身份、空任务检查和日志均留证；首轮保护检查识别到系统conhost后未停止任何进程，核对身份后才完成替换。8000和第10次正式流程保持运行。该提交同时包含工作区同期的断线重连修改，保留并补测页面18项通过、构建通过；其不计入此前166项结果。

I47页面核对：8d99111静态副本通过真实8001读取空工作区与数据源；Browser检查中英文和390×844新建对话框，控件可见。完整科学决定链与最终包仍待正式赢家。中文小标题仍是NEW WORKFLOW、预算设置未进入新建表单，移交I48继续处理。公开Sites前端尚未更新，见 [实服务与页面证据](sources/remaining-modifications-20260912/service-build-update.json)。

I48预算入口：新建页面从实际服务读取默认预算，显式设置候选数、搜索时限、方法调研时限、模型调用绝对总时限及内存/磁盘上限；未展示的后端预算原样保留，自动资源上限仍为null。提交包含明确预算，不增加被试/Run筛选，不改变后台默认值和模型请求配额。模型总时限含数值等待、重试不续期，页面中英文同步；中文小标题已修复。23项页面与双语专项通过、1项API测试通过，前端及Sites构建通过。实际更新后的页面与全流程验收仍待核对。

I48实页面补充：651367e固定静态副本连接实际8001，中英文及390像素窄屏预算输入核对通过，未提交额外工作流。I46完整运行环境已独立安装并通过依赖检查，原环境未修改；冻结f209b40正在进行完整后端回归和310配置验证，尚未切换公共服务。用户明确要求Sites项目访问和发布“先不处理”，I49记为deferred_by_user，保留本地源包；不再将恢复访问列为当前执行项。f209b40推送因GitHub连接失败待重试。

I38矩阵汇总：新增只读audit_unit_matrix，核对结果索引、全部配置身份、源码/引擎/运行环境、驱动和产物哈希；失败和未执行项不丢失，也不沿用历史真实EEG通过状态。11项专项通过（2.99秒），本轮310配置矩阵尚在运行，服务验证注册表未更新。I46冻结f209b40全回归运行到约87%时出现2项失败，完整详情待结束核对，新环境尚未切换。

I46完整环境回归结束：1391通过、3项工作流等待超过120秒、109警告（2103.40秒）。相同冻结f209b40代码和环境下，保持原断言/超时复验3项通过、2警告（136.63秒），各约34/34/68秒；首轮结果保留，不写成单次全通过。复验线程栈曾显示交付证据读取。另修复事件循环退出时先构造to_thread导致未等待协程警告，缺少循环时在创建协程前拒绝；退出与取消期间写入所有权2项通过（2.07秒）。新环境尚待实际切换。

I38实际矩阵发现偏移降采样的Epochs时间轴被Codec误拒绝：FIF将起点写为round(tmin*sfreq)整数，精确起点可在刻度之间。改为验证整数编码起点和精确均匀采样网格，再恢复原时间轴；Raw检查和数值容差不变，错误起点及内部网格篡改仍拒绝。44项测试通过、2警告（170.45秒），原失败配置实际复验通过、最大数值差0（98.431秒）。冻结f209首轮仍运行，失败保留；新引擎全矩阵尚未完成。

I46实服务更新到b27fafb及完整独立环境：API/Worker同版固定，四个检查接口200，Python缺包0，20配置需方法级显式原生资产；运行配置持久保存，Sites发布继续暂缓。初次探测发生在导入期间而连接被拒绝，随后就绪检查和四接口通过。

I27第10次于14:07:58轮询GET返回422，详情为读取search.json时Windows短暂PermissionError。驱动退出后服务正常清理，将运行标记interrupted；原数据/冻结源码/驱动均未变化，15检查点与首轮失败回执保留。新的有界读取重试覆盖Windows CRT只返回errno13的情况，真实独占锁测试在内26项通过（1.34秒）；第一轮25通过/1失败保留。独立恢复驱动4项轮询测试通过（0.90秒），正在以原2906731应用/原环境核对并恢复现有运行，预算不续期，新证据独立保存，不改写首轮failed。

第10次恢复已实际进入running并持续收到200轮询，原预算和15检查点哈希留证。首轮f209b40矩阵310项全部尝试，305通过/5失败，完整回执已通过只读汇总审计；不将旧引擎结果移植为修复版全通过。新增原生真实记录/显式资产验证入口，比较数据、诊断及解码MAT内容，仅归一化MAT文件头和临时目录名称，相关14项及辅助脚本哈希1项测试通过，实际原生执行待进行。b27fafb实际API连接651固定前端的空工作区和预算表单核对通过，未新建重复运行。

58da1f8读取重试修复已实际更新API/Worker，健康/版本/数据源/能力接口均200，原生资产仍须显式绑定；Sites不发布。冻结58da1f8开始S001R04的四原生配置审计，PREP完整数据/MNE状态/诊断/解码MAT对比已通过，其他配置待结束。直接来源输出另补存独立描述符供后续回读，此文件留证补充进入下一轮固定验证。

### 2026-09-13 原生专项结果与MATLAB容器核验

原生专项在58da1f8固定版本完成4项，PREP/RELAX通过；Automagic作者时钟的两次实际数据不同（最大绝对差6.12166067e-05 V），固定种子的实际数据/MNE一致后，核验器读取v7.3中间容器失败。原始4项回执保持不变，记录见`native-profile-pilot-1.json`。

核验辅助脚本补齐v7.3的HDF内容读取，保留所有字段、属性和引用目标，针对性10项测试通过；真实约86MB中间容器已成功读取。当前第二轮全310项使用457bfe6固定版本，包含修复后的降采样与明确原生输入，其冻结脚本不包含新HDF读取器，若再次遇格式缺口将如实保留；新读取器将以独立冻结版本复验原生配置，不能跨版本简单合并为全矩阵通过。原全集搜索仍按原预算恢复执行。

### 2026-09-13 文献工程适配的语义标记

只读检查第10次接入的7–30 Hz仓库方法：原文1–4秒窗在共同评分分支明确替换为0–2秒，原始窗口留在谱系中；滤波保留，但不能称为完整原样复现。发现v2图投影未按fidelity标记工程适配，且重采样说明误称全部源窗口仍保留。已修复这两处元数据，2项回归通过（6.69秒），冻结运行与历史来源不改写。另保留生成解释把FIR zero称为forward-backward的语义错误，不能将通过参数合同等同解释全部正确。证据见`fresh-10-source-semantic-review.json`。

2026-09-13 首次推荐范围清理完成：提交 `67b2564` 保留数值执行前的首次推荐，目录、配方与顺序随后冻结。后续模型调参、步骤编辑/组合、反馈控制及模型影子计划入口已删除，旧实现归档至 `codex/archive-autonomous-preprocessing-20260913`（`96f89a2`）。第10次旧自主运行已取消，旧数据与检查点保留。前端174项、后端最终专项77项与固定副本边界21项通过；前序全回归及复验计数不累加，详见[清理验证](sources/remaining-modifications-20260912/autonomy-retirement.json)。8001 API/Worker 已加载67b2564并拒绝7项旧自主请求；新版真实全集评价和交付仍待验收，Sites继续暂缓。本次没有启动其他修改。

2026-09-13 继续实施：`d6888c8`补齐独立方法库相同请求互斥、完成结果幂等复用与完整构建绑定；34项相关回归和21项固定推荐边界通过，真实供应商故障恢复仍待验收。详见[独立方法库恢复](literature-intake-recovery.md)。第三轮数值矩阵已在匹配旧引擎上只读核验，310项309通过/1失败；PREP、固定种子Automagic及RELAX三个原生配置的S001R04输出、诊断和事件谱系复核通过。作者时钟随机差异保留失败，不调整容差凑通过；不把此证据转换为新应用版本的全集效用或独立科学正确性。见[矩阵审计](sources/remaining-modifications-20260912/matrix-third-pass-audit.json)。本批未重启服务或发布Sites。

2026-09-13 继续实施：`daa5619`修复并发未知记录遮住迟到成功结果，并校验完成回执的动作/序号/来源。29项恢复测试通过，包含真实本地HTTP与独立进程终止；51项相关集成通过。`f8d2960`将官方论文身份的输出合同限定为实际读过的论文ID，证据不足保留未确认，25项相关测试通过。29项恢复与25项论文身份在干净提交分别复验通过，轮次不累加。发现8001离线后已确认无数值任务或待恢复搜索，恢复并更新API/Worker至f8d2960；6个接口正常、7项旧自主请求拒绝，旧失败工作流文件未变。未宣称外部供应商故障恢复、完整新数值闭环或持续可用已完成，Sites仍暂缓。见[检索请求恢复](research-request-recovery.md)。
