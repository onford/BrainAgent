# 独立方法库的并发与恢复

2026-09-13，代码提交 `d6888c8`。

同一用户重复提交同一份文献包时，独立方法库现在使用进程间文件锁串行处理。等待锁时会让出事件循环；取消等待者不会解除正在执行的请求所持有的锁。用户或文献包不同则使用不同锁。锁只覆盖这次文献提取，不参与后续数值候选调度。

成功结果保存为按内容寻址的不可变对象，完成回执同时绑定文献包、应用源码、依赖版本及运行平台。重试先校验回执、结果对象、来源、目标输入和方法引用，然后复用原结果。不能用另一份有效结果替换当前文献包的结果，不能在代码或依赖变化后混用缓存。

源提取与补读仍沿用原有检查点、动作账本及绝对截止时间。重试不重置预算；缺少模型、正文或输入时返回的补充请求不标记为完成，补齐后可继续。同版进程中断会释放文件锁；完成回执写入失败时，已写入的不可变对象不会重复创建，源级提取检查点继续供恢复使用。

旧版独立提取目录没有构建绑定，不能直接加盖新版身份后复用。旧产物保留，返回明确提示要求使用新的调研运行标识。正在进行的提取若发生构建变化，也不会发布完成回执。

验证包含相同文献包并发调用两个方法库实例、进程重建后的结果复用、取消等待、提取取消、完成回执写入故障、未补齐信息、旧版目录、构建变化、内容篡改和结果替换。实际提取集成测试使用可控模型回复，执行文献方法注册及数值 Worker，再验证并发请求与重试没有增加模型调用。

本批相关回归 34 项通过。这里的模型回复为测试夹具，不代表外部供应商真实断网或进程崩溃的完整验收，I13 保留真实运行验收待办。实现保留首次推荐后流程冻结的范围；没有恢复模型后续自主调整。线上/本地正在运行的服务未因本批代码自动重启，Sites 仍按用户要求暂缓。
