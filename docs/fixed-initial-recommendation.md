# 首次推荐与固定流程执行

2026-09-13 用户确认：**保留首次推荐，移除后续自主调整**。本次清理优先于其他修改。

执行顺序为：来源调研与方法提取 → 完整方法目录及共同数据面板冻结 → 模型首次推荐候选顺序 → 保存 `initial-recommendation.json` → 固定参考及推荐流程依序执行 → 评价、确定性选择、报告和交付。

首次推荐只能选择目录中的完整流程 ID，不能提交参数或步骤编辑。它发生在任何数值候选执行前，不接收候选成绩。推荐顺序、目录哈希及完整配方在执行前持久化。恢复复用原推荐；暂时故障只能重试原流程，不能根据错误或成绩生成新配方。预算包括参考流程，仍默认最多 48 个候选、86400 秒及 4 次故障重试；模型总预算、资源上限及到期时间仍随运行保存。

已从当前实现移除后续决策控制器、参数/插入/删除/换序/片段组合编辑合同与执行器、派生候选注册、反馈假设和预测分支调度、模型诊断请求及响应门控、随机/穷举/自适应调度和相应预算。模型可调用的 `shadow_plan` 入口也已移除；测量、检查点、来源和只读解释仍可查看。手工 REST 影子计划是用户显式操作，不再作为 agent 动作提供。原生算法按固定参数执行内部拟合或检测，不属于模型自主调整流程。

新协议为搜索 `version="4"`、工作流 `engine="fixed-recommendation-v1"`。旧搜索与旧工作流在新版服务中仅供读取，不能通过重试恢复自主调整。旧代码保留于本地分支 `codex/archive-autonomous-preprocessing-20260913`（提交 `96f89a2`）；旧数值结果及失败记录不改写。此前第 10 次自主搜索已按用户新要求取消，原始数据与检查点保留，不再续跑自主循环。

不再验收“反馈推动下一候选”“两类模型自主组合”“诊断分支驱动改写”等目标。其历史证据只说明旧版行为。新的验收检查首次推荐有效、执行顺序和配方不受结果影响、恢复不再次推荐、篡改阻止执行，以及评价与交付仍绑定实际物理数据。真实来源提取、数据接入、固定数值评价和交付方面尚未解决的问题继续保留。

此前撤回的接近领域前沿性能、逐被试拟合、逐被试无标签拟合及逐个被试选择预处理流程，均未恢复。Sites 访问与发布继续暂缓。

验证：前端全量 174 项、类型检查、生产构建及中英文页面检查通过。后端首轮全量 1363 通过/22 失败；更新旧夹具后专项 151 通过/4 失败，最终修改模块与剩余失败路径 77 项全部通过。轮次覆盖重叠，不累加，也不声称最后重新运行了完整套件。真实模型在基础目录上完成一次预算内首荐，未执行数值候选、未修改输入；此证据不代表文献效果验证。详见[验证记录](sources/remaining-modifications-20260912/autonomy-retirement.json)。本地服务更新单独记录，Sites 仍暂缓。

本地交付：清理代码提交 `67b2564`。8001 API 与 Worker 已使用同一干净固定副本启动；健康、版本、数据源与能力接口正常，7 项旧策略/预算请求返回 422，未创建数值任务。该固定副本的冻结边界 21 项测试通过。服务源码哈希及原始核验记录见[服务更新](sources/remaining-modifications-20260912/service-build-update-5.json)。其他修改保留原有状态，本次未启动；Sites 继续暂缓。
