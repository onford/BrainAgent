# 检索请求中断、迟到结果与论文身份

2026-09-13：恢复账本修复提交 `daa5619`，论文身份合同修复提交 `f8d2960`。

同一检索或补读请求正在执行时，另一个调用者可能得到“结果未知”的复用记录。原实现之后只查最近一条记录，导致原请求成功落盘后，后续调用仍被较新的未知记录遮住。现在从最近一次实际请求读取结果，复用记录保留当时的状态供审计；原请求仍未保存结果时继续保持未知，不自动重发。

完成回执必须包含且只包含一个观察，其动作与序号必须匹配原预留。成功的阅读还必须带有唯一对应的来源文档，避免其他请求的内容被写入当前预留。动作预算继续按账本中的动作计数，复用动作不伪装成新的网络传输；重启不改变原截止时间，也不恢复已消耗配额。

恢复测试包含真实本地 HTTP、独立 Python 进程和强制终止：响应已返回但尚未保存时终止进程，恢复仍为未知；结果已保存后终止进程，恢复使用原文档；并发调用先记录未知、原请求随后成功时，下一次恢复可取得成功结果。每种情况的服务器都只收到一次请求。测试子进程只允许其父进程创建的一个本地测试 URL，生产环境的来源访问规则保持不变。这些是受控传输与进程故障验收，不是外部检索供应商故障实测。

另一处修复来自真实失败工作流 `c0adbfa348d549d7a1f08b974701290b`：模型将官网引用的采集系统论文填为已识别论文，同时给出 `source_id=null`，被来源校验拒绝。现在模型输出合同只允许选择本轮实际读过的论文 ID；未读到被引用论文时，必须保留 `not_identified` 和空来源 ID，并将官网引用记入事实及缺口。已有逐字引文、官网与论文的联合证据校验继续执行；取得一个合法论文 ID 本身不证明它就是官方数据集论文。

验证：恢复相关 29 项通过，并在干净提交上复验通过；文献提取、模型预算与首次推荐冻结等集成回归 51 项通过；论文身份相关 25 项通过，也在干净提交上复验通过。覆盖有重叠，次数不累加为全量测试数。首轮进程测试因测试正文过短被正常阅读检查拒绝，补足夹具后复验通过，失败日志保留。

本轮开始时 8001 无监听，记录的 API/Worker 进程均已退出。核对后确认没有数值任务及待恢复搜索，仅有上述失败工作流。原状态及日志已归档，API/Worker 已从同一固定 `f8d2960` 恢复，6 个只读接口正常，7 项旧自主策略/预算请求返回 422；旧工作流文件哈希保持不变，未重试其模型调用。退出原因没有由现有证据确定；这次恢复不等于已实现自动启动、持续可用或隧道恢复。Sites 仍暂缓。

证据见[本批验证记录](sources/remaining-modifications-20260912/research-request-recovery-20260913.json)及[服务版本记录](sources/remaining-modifications-20260912/service-build-update-6.json)。完整新版文献与数值交付闭环仍待验收。
