# 预处理策略搜索验收

验收日期：2026-09-09。当前协议为版本 2，运行记录冻结各自的代码、输入、资料、候选目录和评价器。以下运行复用已完成的真实数据接入，不重复声称完成资料调研。

## 自动化验证

后端共 546 项测试分批通过，覆盖结构合同、真实小型 BrainVision 数值执行、全部被试分组交叉验证、无标签个体拟合、CSP 训练隔离、信号/效用预测分别核对、预算与恢复、历史只读、原子发布、报告上下文大小及训练包证据复算。最终文件发布相关回归 140 项通过，包含实际 Windows 读句柄暂时阻止替换的测试。测试用 EDF 解码替身的场景与真实 EDF 验收分开记录。

前端共 93 项测试分批通过，其中最终搜索页 56 项通过；生产构建、Ruff 和差异检查通过。页面检查确认历史结果保留自身的选择语义；新版显示冻结折次、主次评价器、预算及当前候选。109 被试的折次名单默认显示人数，按需展开。

## 真实数据运行

| 运行 | 数据范围 | 协议 | 状态 |
| --- | --- | --- | --- |
| [完整范围搜索](http://127.0.0.1:5173/searches?id=78b74494394e4780aae16e243c01ac7d) | 109 位被试，R04/R08/R12，共 327 个记录 | 全部被试 5 折，6 候选上限，3600 秒 | 完成 4 候选，1 候选因时间预算中止 |
| [小规模流程验收](http://127.0.0.1:5173/searches?id=56c1cd19d4a242799a3bcc93947335f6) | S001–S005，R04/R08，共 10 个记录 | 5 折，6 候选上限，3600 秒 | 完成 6 候选，按预算停止 |

两次搜索均使用项目配置的真实 LLM。小规模运行已验证错误指标路径被拒绝、模型修正提案、按实际成绩继续比较公共处理与个体对齐；执行错误和被拒提案不会成为零分结果。

## 小规模结果与训练包

该运行使用 150 个有效试次，真实 LLM 为 `deepseek-v4-flash`。累计 501.2 秒、7 次模型调用、7 次提案、0 次补充阅读；包含一项错误指标引用和一次空答复，均记录为拒绝。所有成功候选完整覆盖相同的 150 个试次。

| 候选 | CSP/LDA 开发 BA | 次要对照 BA |
| --- | ---: | ---: |
| 8–30 Hz、平均参考 | 57.74% | 61.58% |
| 1–40 Hz、平均参考 | 52.22% | 59.57% |
| 8–30 Hz、保留采集参考 | 59.44% | 58.41% |
| 8–30 Hz、逐被试 EA（选中） | 63.10% | 62.52% |
| 4–40 Hz、逐被试 EA | 60.99% | 61.14% |
| 8–30 Hz、条件 EA，阈值 10 | 54.99% | 59.43% |

条件 EA 对 2/5 位被试触发，对其余被试只统一尺度；触发比例和逐被试诊断已在页面核对。最高主分数对应统一 EA，选择并未强制偏好条件策略。单次小样本运行不能证明这些策略的一般优劣。

实际交付投影调用生产代码的选择、报告和导出函数，复用源调研/接入资料，不重新执行完整主工作流。独立目录内生成 `X.shape=[150,64,321]`、无量纲训练包：逐 trial 对比 scored 数组的 float32 转换相等；分别从源数组和导出数组重新拟合五折 CSP/LDA，150 条预测及宏平均分数均一致。ZIP 的 55 个文件通过哈希、大小与 CRC 核验。train=150，validation/test=0，完整折和逐 trial 预测随包保存。

- [训练包](../backend/workspace/acceptance/56c1cd19d4a242799a3bcc93947335f6/training-data.zip)
- [报告](../backend/workspace/acceptance/56c1cd19d4a242799a3bcc93947335f6/report/report.html)
- [机器可读审计](../backend/workspace/acceptance/56c1cd19d4a242799a3bcc93947335f6/audit.json)

## 完整范围结果与训练包

共同面板为 4918 个有效试次，五折开发组大小为 22/22/22/22/21，每位被试恰好一次折外预测。累计耗时 3600.013 秒，5 次真实模型调用、5 次提案、0 次补充阅读、1 次故障重试。数值子进程峰值内存约 1.10 GiB，搜索目录约 14.49 GiB。

| 候选 | CSP/LDA 开发 BA | 次要对照 BA | 状态 |
| --- | ---: | ---: | --- |
| 8–30 Hz、平均参考 | 63.27% | 62.94% | 已评价 |
| 8–30 Hz、保留采集参考 | 63.03% | 59.17% | 已评价 |
| 8–30 Hz、逐被试 EA | 69.99% | 65.00% | 已评价 |
| 4–40 Hz、逐被试 EA（选中） | 70.66% | 63.99% | 已评价 |
| 4–40 Hz、条件 EA，阈值 10 | — | — | 时间预算耗尽，未参与选择 |

选中候选相对参考提高 7.38 个百分点。四个成功候选均完整覆盖 4918/4918 试次；逐被试主次分数、折归属、输入数组和诊断摘要经独立复算核对。搜索清单中 37,157 个文件的大小和哈希全部匹配，无漏列。第四候选期间出现一次 Windows 状态文件替换占用，通过正式恢复接口使用原预算的一次重试；已有候选未重算，截止时间未重置。

生产选择、报告和导出函数生成 `X.shape=[4918,64,321]` 的无量纲训练包。逐 trial 与实际评分数组的 float32 转换一致；从源数组和导出数组分别重新拟合五折 CSP/LDA，4918 条预测及宏平均 BA 全部一致。ZIP 中 1110 个文件通过哈希、大小和 CRC 核验。train=4918，validation/test=0；全部折、预测及个人变换矩阵随包保存。导出复用已有调研接入，无额外 LLM 调用，源工作流与搜索产物前后哈希不变。

- [训练包](../backend/workspace/acceptance/78b74494394e4780aae16e243c01ac7d/training-data.zip)
- [报告](../backend/workspace/acceptance/78b74494394e4780aae16e243c01ac7d/report/report.html)
- [交付审计](../backend/workspace/acceptance/78b74494394e4780aae16e243c01ac7d/audit.json)
- [候选数值审计](../backend/workspace/acceptance/fullsearch/audit.json)
- [终态、预算与文件完整性审计](../backend/workspace/acceptance/fullsearch/verification.json)

## 结论范围

主指标为各被试折外平衡准确率的等权均值，评价器为 CSP＋收缩 LDA；对数方差＋逻辑回归为次要对照。个体适配读取该人的无标签整批试次，属于离线转导协议。开发成绩反复用于候选选择，配对区间仅描述当前开发样本，不能用作独立泛化结论。

过程数据和训练包位于被 Git 忽略的 workspace 中；实现、Schema、模板、测试和当前验收说明纳入版本控制。
