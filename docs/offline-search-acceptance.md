# 预处理搜索固定版本验收

核查日期：2026-09-10。当前范围为固定机制、运行可靠性和证据核验；不继续自动探索。历史算法与验收说明由 Git 保存，既有运行保留自身的冻结代码和评价协议。

## 固定机制

方法由可执行算子组合而成。基础与文献方法共七个起点，允许域内调参、增删可选算子、合法换序和无标签适配变化。结构 Schema、输入阶段、数据权限和硬约束决定能否编译；挑战软先验需要明确理由与可检验预测。

主指标固定为 CSP-LDA、FBCSP、TS-LR 各自被试平均 BA 的等权均值，三者必须完整覆盖冻结开发面板。三个模型对照、25 项质量指标和14项半合成重建指标独立呈现，不事后调权重决定胜者。详细含义见[评价阅读说明](preprocessing-evaluation.md)，运行及产物合同见[机制说明](offline-preprocessing-search.md)。

## 全集数值验证

EEGMMIDB 的109名被试、R04/R08/R12共327条记录，输出4918个左右手运动想象试次。源目录的全部Run仍保留在数据接入清单，任务评价使用有明确语义映射的三个Run。

- 原始EDF与接入BIDS的327条记录逐点电压、通道顺序、采样率和形状核对一致。
- 坏通道修复方案完成327条记录。
- 条件ASR方案完成327条记录：324条应用ASR，3条校准片段不足30秒，按固定条件保留该步骤输入；事件、形状及4918个试次保持一致。
- 条件方案中实际应用的记录与严格ASR结果逐文件一致，跳过记录与无ASR修复结果一致；信号与事件文件SHA各327/327通过。
- 3条校准不足记录为S021R08（17.44375秒）、S021R12（15.0125秒）、S022R04（27.0125秒）。它们没有被排除，也不能被计为实际ASR清理。

对应本地数值证据：`.local/preflight/all-source-voltage-audit.json`、`.local/preflight/cleaning-conditional-v5/final-verification.json`及`conditional-parity.json`。生成数据位于Git忽略目录。

## 已测全集参考结果

[运行 b10fce44b115403bb10df713eaf19958](http://127.0.0.1:5173/searches?id=b10fce44b115403bb10df713eaf19958)使用109名被试、327条记录、4918个合格试次及固定五折被试划分。首个8–30 Hz平均参考方案完成全部模型预测。

| 指标 | 被试平均 BA |
| --- | ---: |
| CSP-LDA | 63.27% |
| FBCSP | 59.77% |
| TS-LR | 62.85% |
| 三模型平均选择分数 | 61.96% |
| FgMDM 对照 | 64.72% |
| 逐频带 EA-FBCSP 对照 | 69.08% |
| 对数方差逻辑回归对照 | 62.94% |

这些是开发成绩，不能作为独立泛化结论或接近领域SOTA的证据。参考方案的质量评价完整，原运行的重建评价为部分可用。第二个候选在文件发布期间中断；该运行没有完成广泛探索，也没有生成最终训练交付包。历史回执保持不变，定向修复验证不覆盖旧结果。

## 自动化验证范围

资源扫描与搜索控制回归36项通过，涵盖原子发布时文件消失、真实磁盘不足、取消、预算和恢复。重建回归110项通过，覆盖污染注入、信号保留对照、NumPy样本数与尾块边界、事件及清单一致性。

在独立目录定向重放历史失败的38项重建实验，全部完成；注入数值与哈希保持一致，6662个受保护历史和输入文件哈希未变。这是失败用例的修复验证，不是新的109人完整搜索或重新合并的评价成绩。验证记录：`.local/preflight/reconstruction-partial-20260910/validation.json`。

Windows适配文件与认知流程定向回归64项通过，包含240字符目录下的读写及哈希核验；测试使用默认临时目录。多维评价、实际清理算子组合、资源恢复、子进程退出和评价完整性联测31项通过。Ruff和差异检查通过；本地页面、健康检查及历史搜索API均返回200。

导出验证使用包内预测独立复算每位被试的类别召回率，再计算每种模型的BA与三模型平均数，分别核对CSP锚点和选择分数；不能以单模型分数替代主指标。完整性检查保留输入、配方、计划、执行、探针与评价文件的哈希绑定。

## 当前边界

当前支持的自动清理是注明适配差异的坏通道处理与ASR，不声称完整复现PREP或所有文献流程。研究目录包含未接入的操作和未实现指标，研究覆盖数量不能作为实现数量。半合成参考来自真实EEG，包含未知生理与伪迹成分，不是纯净神经真值；物理阶段实验不认证EA后的空间表示保真。

固定版本不恢复旧搜索。执行或评价代码变化后，旧运行仍用于阅读与审计；后续探索需在明确开始时创建新的冻结运行。
