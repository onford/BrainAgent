# 核心交付顺序

2026-09-13，按用户“先完成核心内容”的要求重新排序。本文件替代旧台账的执行顺序，不改变历史结果或把部分完成项自动关闭。

分类时的 50 个未关闭条目分为 **34 个核心条目、15 个后置优化与扩展条目、1 个暂缓条目**。核心中的 I19、I22、I37、I41 当时已有实现或范围结论，需要核对证据和收口；其余 30 项仍有实现或验证工作。条目之间有依赖，按下面六个交付环节推进，不再以扩充外围功能作为进入真实主链的前提。

| 顺序 | 核心环节 | 台账编号 | 本轮完成条件 |
| --- | --- | --- | --- |
| 1 | 固定版本和可运行入口 | I13、I14、I32、I33、I37、I45、I46 | 代码与环境身份明确；当前入口能启动任务；预算持久化，取消与恢复受原期限约束；真实运行的时间、资源及失败可追溯 |
| 2 | 真实数据与来源核对 | I06、I40、I41、I42 | 完整 EEGMMIDB 目标数据按事实核对；官方验证、缺失和排除依据可查；不根据模型猜测排除对象 |
| 3 | 文献进入首次推荐 | I01、I03、I05、I07、I09、I12、I16、I17、I18 | 真实可访问来源经共同入口提取；参数、依赖、规则与来源绑定；实质文献方法自然进入首次推荐；保留不能执行的原因 |
| 4 | 固定流程执行正确 | I02、I08、I19、I21、I22、I36、I38、I39 | 推荐在数值评价前冻结；只执行冻结候选；共享预处理与物理数组、事件和评分视图一致；对实际使用的操作核对正确性；诊断只读，手动入口边界有效 |
| 5 | 完整评价与可复现交付 | I23、I24、I27、I43、I44 | 全量目标面板、EEGNet 五折与三种子及 CSP-LDA 对照完成；选中模型、评分、训练记录、预处理及 ZIP 对应同一版本；来源与实际影响复核；结论不超出证据 |
| 6 | 用户入口验收 | I47 | 当前界面能完成创建、查看首次推荐、固定执行进度、结果与下载；失败和限制可见；不提供后续模型自主调整入口 |

这里的完成条件是当前运动想象交付的核心切片。例如 I09 的全部经典方法、I38 的全部配置覆盖、I39 的全面独立算法验证可能仍有后续范围；核心切片通过不等于整条问题自动关闭。已有 309/310 的旧配置矩阵不能替代新版真实交付；已知失败和未授权依赖继续显式阻塞对应配置。

后置工作分为两类，均不阻塞上述核心链路，但不因此撤销：

| 类别 | 条目 | 范围 |
| --- | --- | --- |
| 细节优化与运行便利 | I10、I11、I48、I50、I51、I52 | 扩大调研覆盖与停止策略优化、细化证据分级、全站文档及双语一致性、持续在线及隧道恢复、匿名共享资源治理、离线快照；核心所需的真实引用、权限边界、可重启入口和复现交付仍须先完成 |
| 科学验证与能力扩展 | I15、I20、I25、I26、I28、I31、I57、I58、I59 | 更广神经先验和保护验证、已知源扩展、鲁棒性与不确定性、独立泛化、因果收益拆分、ERP/SSVEP/静息态、更多数据格式及科学分析路线；这些是实质扩展，不称为小修小补 |
| 用户暂缓 | I49 | Sites 项目访问与发布，继续遵照“先不处理” |

已撤回的 9 项、已归档替代的 I60 和已完成的 R01 不计入以上 50 项。不恢复前沿性能目标、逐被试拟合/个体预处理或后续模型自主调整。

当前执行入口：先核对新版验收驱动与“首次推荐后固定执行”的合同，再以新的独立运行目录启动真实主链。保留历史失败工作流及取消的旧自主任务，不跨版本恢复。完整运行未结束前不把“已启动”登记为通过。

本轮已执行：固定副本 `722fe95` 的 32 项相关回归通过；新工作流 `ea709a3bf4ad48a7af3373494cde6957` 已开始真实数据调研，完整 109 人范围，候选上限 4（含参考），数值预算 12 小时、模型绝对总时限 16 小时。执行使用独立目录中的正式 API 入口；不是 8001 服务已有任务，也未发布 Sites。运行结束后已衔接选中模型与训练 ZIP 的独立核验，仍需来源语义和用户入口复核。详见[启动证据](sources/remaining-modifications-20260912/core-delivery-start-20260913.json)。这些是进行中状态，不表示 34 项核心已完成。

后续验收：本轮数据门检及固定后端全量 1426 项回归通过，I13、I19、I22、I37、I41 已正式关闭；核心剩余 29 项未正式关闭，全部未关闭条目剩余 45 项。独立方法库入口完成真实模型提取和重复请求复用核验，数值流程一致及工程复核差异均有记录。首次推荐已冻结四个流程并进入参考流程的 EEGNet 评价。已知信号检查覆盖实际使用的 FIR/IIR、平均参考、重采样及分段；来源参数复核同时记录了两处模型文字说明的更正，原始记录保留。详见[核心验证记录](core-delivery-validation.md)。

2026-09-14 用户要求继续核心任务，已从上述暂停点恢复。原计算及模型/ZIP 核验保留原范围和预算。前两个固定流程完成，第三个评价中；前两个流程分别完成全 327 记录的质量回执复核，318 记录差值逐值相符，9 条 128 Hz 记录保留不可比原因。I01、I06 本次收口后，核心正式关闭 7 项、剩余 27 项，全部未关闭条目剩余 43 项。统一发布候选 `81e589e` 整合已有加载修复及固定执行说明，前端 174 项/类型检查/构建通过，后端全量回归进行中。最终结果接入及页面验收仍需完整计算结束。

同日后续：`81e589e` 后端全量 1437 项通过，已安装到现有 8001 API/Worker 和 5173 前端；接口、资源哈希、双语表单及历史只读边界通过。前三个共享计划没有记录特定参数或预处理拟合，四个反向控制被拒绝。待固定数值运行结束后，使用已验证的完成结果迁移工具接入；保留数值执行原版本和原始树，显式记录本地身份映射。完整模型、ZIP、第四流程及实际结果页面仍是核心验收的剩余步骤。
